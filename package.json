{
  "name": "notepad-zip",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types",
    "test": "npm run build && node scripts/zip-test.mjs && node scripts/smoke.mjs && node scripts/client-logic-test.mjs",
    "build": "wrangler deploy --dry-run --outdir=dist"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260905.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.129.0"
  }
}